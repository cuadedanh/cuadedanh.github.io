# cuadedanh.github.io
Dùng AI code thì phải dúng claude tốt
nhớ đặt tên file zip phải đúng chuẩn ID-version. Version ở tên file phải giống trong addon.xml. nếu không sẽ báo lỗi khi cài
tên file player json phải viết liền không khoảng trắng.. nếu không sẽ báo lỗi hoặc setdefault vẫn hỏi chọn flayer


1.4 sửa lại phần hiện size, sắp xếp size
1.5 tạo file strm link fshare 
1.6 tạo file nfo thông tin codec, nếu có title truyền vào thì ghi vào nfo không thì để trống title
1.6.1 sửa lỗi không mở được smb khi chọn thư mục tạo file
1.7 sửa phần tên nếu có dấu ngoặc (thuyet minh) về sau phần năm để dễ nhận diện, phần sau năm giữ nguyên, bỏ file nfo
1.8.0 Lưu lại thư mục lưu strm. sửa lại hàm player và showfshalink để play film qua tmdb helper hay bất cứ cái chọn fshare đều ghi nhận trên trakt, scan 1 file không cần scan cả thư viện. Sửa để có thể tìm tvshow chuẩn 
1.9.0 thêm chức năng download fshare, tên file theo quy tắc file strm, lưu cùng thư mục. player 1.9 đã sửa để tmdb có cài tiếng việt thì vẫn search fshare theo ngôn ngữ gốc tiếng anh.
